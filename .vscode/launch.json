{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug MbedTLS",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": true
            
        }
    ]
}